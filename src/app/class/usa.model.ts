export class Usa {
    state: string;
    case: string;
    death: string;

    constructor(state:string, cases:string, death: string) {
        this.state = state;
        this.case = cases;
        this.death = death;
    }
}